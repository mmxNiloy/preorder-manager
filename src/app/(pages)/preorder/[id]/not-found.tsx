import { PreorderFormNotFound } from "@/components/custom";

export default function NotFound() {
  return (
    <div className="mx-auto flex min-h-screen w-full max-w-4xl flex-col gap-6 px-6 py-8">
      <PreorderFormNotFound />
    </div>
  );
}
